# Local server
PORT=3000

# Do not commit real API keys or OAuth tokens.
# Gmail credentials are managed inside n8n Credentials.
# Brevo/SendGrid keys, if used later, should be stored in n8n or Render environment variables.
# BREVO_API_KEY=
# SENDGRID_API_KEY=
